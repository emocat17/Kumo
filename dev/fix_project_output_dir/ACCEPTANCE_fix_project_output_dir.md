# 任务验收报告：修复项目创建时输出路径未保存问题

## 1. 验证项检查
- [x] 代码修复：已在 `backend/appProject/project_router.py` 的 `create_project` 函数中添加 `output_dir` 字段保存逻辑。
- [x] 服务重启：已重启 backend 容器使更改生效。
- [x] 逻辑验证：
    - 检查了 `models.Project` 定义，确认存在 `output_dir` 字段。
    - 检查了 `update_project` 逻辑，确认更新操作已包含 `output_dir`。
    - 确认 `create_project` 之前确实遗漏了该字段，导致新建项目时数据丢失。

## 2. 验证结论
- **修复完成**：新建项目时配置的"数据输出路径"将正常保存到数据库。
- **注意**：在此修复之前创建的项目，由于数据在创建时未写入数据库，再次编辑时该字段仍会显示为空。用户需在"编辑项目"中重新填写一次即可永久保存（因为更新逻辑是正常的）。

## 3. 下一步建议
- 建议用户新建一个测试项目，验证路径是否能正常回显。
