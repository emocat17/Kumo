![1765088537827](image/前端样式统一规范/1765088537827.png)![1765088544320](image/前端样式统一规范/1765088544320.png)![1765088552392](image/前端样式统一规范/1765088552392.png)![1765088557237](image/前端样式统一规范/1765088557237.png)![1765088645711](image/前端样式统一规范/1765088645711.png)# 前端样式统一规范

为确保项目各页面（定时任务、项目、Python环境、Python版本）视觉风格一致，特制定本规范。所有样式已封装在 `front/src/styles/common.css` 中，并在 `main.ts` 中全局引入。

## 1. 页面布局 (Page Layout)

所有页面应遵循统一的容器结构。

```html
<template>
  <div class="page-container">
    <PageHeader title="..." description="...">
      <template #actions>
        <!-- 页面级操作按钮 -->
        <button class="btn btn-primary">...</button>
      </template>
    </PageHeader>

    <!-- 筛选/搜索栏 -->
    <div class="filter-bar">
      <div class="search-wrapper">
        <i class="icon-search">🔍</i>
        <input class="search-input" placeholder="..." />
      </div>
      <!-- 其他筛选器 -->
    </div>

    <!-- 内容区域 (Grid 或 Table) -->
    <div class="grid-container">
       <!-- 卡片 -->
    </div>
  </div>
</template>
```

- **容器类**: `.page-container` (最大宽度 1200px, 居中, Flex 纵向布局)
- **筛选栏**: `.filter-bar` (白色背景, 圆角, 边框, Flex 布局)

## 2. 按钮样式 (Buttons)

按钮应根据内容自动调整宽度。

- **基础类**: `.btn`
- **变体**:
    - `.btn-primary`: 主操作（蓝色背景，白字）
    - `.btn-secondary`: 次要操作（灰色背景，深灰字）
    - `.btn-danger`: 危险操作（红色背景，红字）
    - `.btn-sm`: 小尺寸按钮（适用于卡片内部操作）

```html
<button class="btn btn-primary">新建项目</button>
<button class="btn btn-secondary">刷新</button>
<button class="btn btn-danger btn-sm">删除</button>
```

## 3. 输入框样式 (Inputs)

搜索框和表单输入框风格统一。

- **搜索框组合**: `.search-wrapper` (包含图标和输入框的容器) + `.search-input`
- **表单输入框**: `.form-input`
- **下拉选择**: `.form-select`
- **文本域**: `.form-textarea`

```html
<!-- 搜索框 -->
<div class="search-wrapper">
  <i class="icon-search">🔍</i>
  <input class="search-input" placeholder="搜索..." />
</div>

<!-- 表单输入 -->
<input class="form-input" placeholder="请输入..." />
```

## 4. 卡片样式 (Cards)

- **容器类**: `.grid-container` (Grid 布局, 自适应列宽)
- **卡片类**: `.card`
    - `.card-header`: 标题区域
    - `.card-body`: 内容区域
    - `.card-footer`: 底部区域 (可选)
    - `.card-title`: 标题样式

## 5. 数据表格 (Data Tables)

- **容器类**: `.table-container` (支持横向滚动)
- **表格类**: `.data-table`
    - 自动处理表头背景、边框颜色和行悬停效果。

```html
<div class="table-container">
  <table class="data-table">
    <thead>
      <tr>
        <th>标题</th>
        <th>操作</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>内容</td>
        <td><button class="btn btn-sm">操作</button></td>
      </tr>
    </tbody>
  </table>
</div>
```

## 6. 状态标签 (Status Badges)

- **基础类**: `.status-badge`
- **状态变体**:
    - `.ready` / `.active` / `.success`: 绿色
    - `.installing` / `.running`: 蓝色
    - `.error` / `.failed` / `.deleting`: 红色
    - `.paused`: 橙色
    - `.finished`: 灰色

```html
<span class="status-badge active">运行中</span>
```
